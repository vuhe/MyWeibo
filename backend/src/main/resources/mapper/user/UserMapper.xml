<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.zhuhe.weibo.mapper.user.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="Base" type="site.zhuhe.weibo.entity.user.User">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="password" property="pwd"/>
    </resultMap>

    <select id="loadUserByUsername" resultMap="Base">
        SELECT `id`, `name`, `password`
        FROM `user`
        WHERE `name` = #{username}
    </select>

    <select id="getUser" resultMap="Base">
        SELECT `id`, `name`, `password`
        FROM `user`
        WHERE `id` = 1
    </select>

</mapper>